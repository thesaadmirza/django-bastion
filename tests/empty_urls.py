"""A URLconf with bastion deliberately not included.

Used to prove the doctor notices, rather than reporting success on a project
where the callback is unreachable.
"""

from __future__ import annotations

urlpatterns: list = []
